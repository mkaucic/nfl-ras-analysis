{
  "name": "nfl-ras-analysis",
  "version": "1.0.0",
  "description": "NFL RAS Analysis Frontend",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd frontend && npm start",
    "test": "cd frontend && npm test"
  },
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  }
}
